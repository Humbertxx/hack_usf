[pytest]
# Repo root must be on path so `import cv` resolves (cv/ is not the package root).
pythonpath = ..
testpaths = tests
markers =
    integration: downloads weights / runs full ML stack (optional on CI)
