# Local camera client for the CV server. From repo root:
#   pip install -r capture/requirements.txt
#
# Uses full opencv-python (not headless) for reliable camera access on macOS.

opencv-python>=4.10.0
httpx>=0.27.0
numpy>=1.26.0
